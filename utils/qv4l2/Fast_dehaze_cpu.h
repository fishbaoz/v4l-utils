#ifndef FAST_DEHAZE_CPU_H
#define FAST_DEHAZE_CPU_H

#include <iostream>
#include <time.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include <errno.h>
#include <assert.h>
#include "opencv2/opencv.hpp"

using namespace cv;
using namespace std;

#define my_min(x, y) ((x) < (y) ? (x) : (y))
#define my_max(x, y) ((x) > (y) ? (x) : (y))

#endif
